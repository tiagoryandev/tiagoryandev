import React from "react";
import type { NextPage } from "next";

const Home: NextPage = () => {
	return (
		<h1 className="text-xl text-center font-bold">Hello, Word!</h1>
	);
};

export default Home;